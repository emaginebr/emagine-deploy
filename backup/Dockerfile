# Trecho removido do Dockerfile principal — COPYs dos builds do easysla.
# Preservado para referência / eventual rollback.
# Originalmente nas linhas 9–10 do Dockerfile.

COPY builds/easysla-site /var/www/easysla.com/home
COPY builds/easysla-app /var/www/easysla.com/app

# ----------------------------------------------------------------------
# Trecho removido do Dockerfile principal — COPY do build do nochainswap.
# Originalmente na linha 9 do Dockerfile.

COPY builds/nochainswap /var/www/nochainswap.org/home
