services:
  emagine-app:
    build: .
    container_name: emagine-app1
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx-local.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - emagine-network

networks:
  emagine-network:
    external: true
